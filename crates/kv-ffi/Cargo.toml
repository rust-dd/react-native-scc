[package]
name = "kv-ffi"
version.workspace = true
edition = "2024"
publish = false

[lib]
name = "scc_kv_ffi"
crate-type = ["staticlib", "rlib"]

[dependencies]
kv-core = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
